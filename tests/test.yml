---
- hosts: mongodb
  remote_user: root
  vars:
    is_standalone: True
    is_replica_set: True
    whether_uninstall: False # Whether to uninstall the original service and then install it
  roles:
    - dba_ray.mongodb