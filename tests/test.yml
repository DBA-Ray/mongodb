---
- hosts: mongodb:slave_monitor1
  remote_user: root
  vars: 
    #is_standalone: True
    #is_replica_set: True
    percona_tools: True # Whether to install percona tools
  roles:
    - dba_ray.mongodb