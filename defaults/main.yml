---
# defaults file for dba_ray.mongodb
# Directory path
mongodb_version: 4.4
fstab_src: /dev/vdb
install_dir: /data
basedir: "{{ install_dir }}/mongodb"
shelldir: "{{ install_dir }}/shell"
monitordir: "{{ install_dir }}/monitor"
datadir: "{{ basedir }}/data"
keydir: "{{ basedir }}/key"
etcdir: "{{ basedir }}/etc"
logdir: "{{ basedir }}/log"
socket: /tmp/mongodb.sock
# Basic parameters
replsetname: "ttjb_rs1"
mongodb_port: 27017